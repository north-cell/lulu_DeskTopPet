[project]
name = "lulu-desktop-pet"
version = "0.1.0"
description = "A Windows-first capybara Lulu desktop pet MVP."
requires-python = ">=3.10"
dependencies = ["PySide6>=6.7"]

[project.scripts]
lulu-pet = "lulu_pet.app:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
